{
  "name": "delete-untracked-git-branches",
  "version": "1.0.1",
  "description": "Delete all those annoying untracked local Git branches in one fell swoop",
  "keywords": [
    "git",
    "untracked",
    "branches",
    "branch",
    "delete"
  ],
  "author": {
    "name": "JP Strydom",
    "email": "jpstrydom8@gmail.com"
  },
  "license": "MIT",
  "main": "dist/index.js",
  "bin": {
    "delete-untracked-git-branches": "./dist/index.js"
  },
  "files": [
    "dist/",
    "LICENSE",
    "package.json",
    "README.md"
  ],
  "repository": {
    "type": "git",
    "url": "git+https://github.com/JPStrydom/Delete-Untracked-Git-Branches.git"
  },
  "bugs": {
    "url": "https://github.com/JPStrydom/Delete-Untracked-Git-Branches/issues"
  },
  "homepage": "https://github.com/JPStrydom/Delete-Untracked-Git-Branches#readme",
  "scripts": {
    "build": "node build.js",
    "clean": "rimraf dist test-repo test-remote-repo.git \"delete-untracked-git-branches-*.tgz\" --glob",
    "posttest": "npm run clean",
    "prebuild": "npm run clean",
    "prepack": "npm run build",
    "prepare": "husky",
    "pretest": "npm pack",
    "prettier": "prettier --write \"./**/*.{js,json,md}\"",
    "test": "node test.js"
  },
  "dependencies": {
    "yargs": "^17.7.2"
  },
  "devDependencies": {
    "esbuild": "^0.24.0",
    "husky": "^9.1.6",
    "lint-staged": "^15.2.10",
    "prettier": "^3.3.3",
    "rimraf": "^6.0.1"
  }
}
